<Button x:Class="WaveButton.MyWaveButton"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WaveButton"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Button.Template>
        <ControlTemplate TargetType="Button">
            <Grid Background="{TemplateBinding Background}" MouseLeftButtonDown="Grid_OnMouseLeftButtonDown">
                <Border>
                    <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalAlignment}" VerticalAlignment="{TemplateBinding VerticalAlignment}"></ContentPresenter>
                </Border>
                <Path Fill="Black" x:Name="MyPath">
                    <Path.Data>
                        <EllipseGeometry x:Name="MyEllipseGeometry" RadiusX="0" RadiusY="{Binding RelativeSource={RelativeSource Self},Path=RadiusX}"></EllipseGeometry>
                    </Path.Data>
                </Path>
            </Grid>
        </ControlTemplate>
    </Button.Template>
</Button>
